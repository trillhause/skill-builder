'use client';

import { TesterTab } from '@/types/workspace';

interface TesterThreadsSidebarProps {
  tab: TesterTab;
}

export default function TesterThreadsSidebar({ tab }: TesterThreadsSidebarProps) {
  return (
    <div className="sidebar-content">
      <div style={{ padding: '16px', color: 'var(--text-secondary)' }}>
        <h4 style={{ marginBottom: '12px', fontSize: '13px', fontWeight: 600 }}>Test Threads</h4>
        <p style={{ fontSize: '12px' }}>
          Tester threads sidebar - coming soon
        </p>
        {/* TODO: Implement test threads list per wireframe */}
      </div>
    </div>
  );
}
