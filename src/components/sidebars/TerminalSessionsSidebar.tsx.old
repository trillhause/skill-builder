'use client';

import { TerminalTab } from '@/types/workspace';

interface TerminalSessionsSidebarProps {
  tab: TerminalTab;
}

export default function TerminalSessionsSidebar({ tab }: TerminalSessionsSidebarProps) {
  return (
    <div className="sidebar-content">
      <div style={{ padding: '16px', color: 'var(--text-secondary)' }}>
        <h4 style={{ marginBottom: '12px', fontSize: '13px', fontWeight: 600 }}>Sessions</h4>
        <p style={{ fontSize: '12px' }}>
          Terminal sessions sidebar - coming soon
        </p>
      </div>
    </div>
  );
}
