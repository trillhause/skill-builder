Analyze @.content/sprints.json and @progress.txt 

Pick up the next sprint that is pending.

We want to create tasks for the next sprint. Create a sprint-<number>.json for the next sprint. The sprint-<number>.json should follow similar structure to all sprint files in the @.content/sprints. 

Example @.content/sprints/sprint-1.json. 
